# 每日开发进度记录

本文档用于记录企业知识库 AI 客服 Agent Demo 的每日开发进度、关键变更、验证结果和下一步计划。

## 项目当前阶段规划

1. 基础聊天闭环
2. 知识库上传与文档解析
3. RAG 核心闭环
4. 客服基础功能
5. 管理后台
6. Tool Calling / 企业系统模拟

---

## 2026-07-07 昨日开发摘要

### 开发阶段

- 阶段 0：需求收敛与开发计划整理
- 阶段 1：基础聊天闭环

### 完成内容

- 明确项目定位为“通用企业智能客服 Agent Demo”。
- 明确基础版目标：先做通用、可运行、可演示、可扩展的基础智能客服，而不是一开始做复杂企业级 SaaS。
- 明确 RAG 是核心能力，但第一版只做最常见、基础、通用的 RAG，方便未来迁移到其他企业项目。
- 将开发路线整理为 6 个阶段：
  1. 基础聊天闭环
  2. 知识库上传与文档解析
  3. RAG 核心闭环
  4. 客服基础功能
  5. 管理后台
  6. Tool Calling / 企业系统模拟
- 明确技术栈保留：
  - React
  - FastAPI
  - DeepSeek API
  - Embedding
  - Chroma
  - PostgreSQL + pgvector 作为 V1 增强方向
  - RAG
  - 简单 Tool Calling
- 完成阶段 1 基础聊天版本：
  - React 聊天界面
  - FastAPI 后端接口
  - DeepSeek / OpenAI-compatible LLM 调用封装
  - 流式输出
  - 无 API Key 时本地演示回复
- 初始化 Git 仓库并首次推送到 GitHub。

### 关键变更

- 更新需求文档，收敛为“通用基础版 RAG 智能客服 Agent”。
- 更新开发计划，改为 6 阶段推进。
- 更新 README，补充项目定位、技术栈和启动说明。
- 建立前后端基础项目结构。

### 验证结果

- 前端页面可访问。
- 后端健康检查接口正常。
- 前端可发送消息并显示后端回复。
- 首次 GitHub 推送成功。

### 遇到的问题

- 初次推送时 GitHub HTTPS 凭据无效，后通过本机 GitHub CLI keyring 账号完成推送。
- 后续检查发现部分中文内容存在编码损坏风险，需要统一清理为正常 UTF-8。

### 下一步

- 修复阶段 1 中文文案和日志中的编码问题。
- 进入阶段 2：知识库上传与文档解析。

---

## 2026-07-08 今日开发摘要

### 开发阶段

- 阶段 1 收尾修复
- 阶段 2：知识库上传与文档解析

### 完成内容

- 修复阶段 1 中的中文编码问题：
  - 后端 LLM system prompt
  - 本地演示回复
  - 前端聊天界面中文文案
  - 开发计划和开发日志文档
- 完成阶段 2 最小闭环：
  - 文档上传接口
  - 文档列表接口
  - 文档解析预览接口
  - 文档删除接口
  - PDF / TXT / Markdown 支持
  - 简单文本清洗
  - 本地文件和 JSON 元数据保存
  - 前端知识库管理区域
- 明确阶段 2 状态口径：
  - 当前同步处理只使用 `processed`、`failed`
  - `pending`、`processing`、`indexed` 放到阶段 3 或 V1 增强
- 更新 `.gitignore`，忽略本地上传数据目录 `backend/data/`。

### 关键变更

- 新增 `backend/app/api/documents.py`
- 新增 `backend/app/services/document_service.py`
- 新增 `backend/app/schemas/document.py`
- 更新 `backend/app/main.py`，注册文档管理接口
- 更新 `backend/requirements.txt`，加入：
  - `python-multipart`
  - `pypdf`
- 更新 `frontend/src/App.tsx`，增加知识库管理工作区
- 更新 `frontend/src/styles.css`，支持聊天区和知识库区并排布局
- 更新 `docs/development_plan.md`，恢复正常中文并修正阶段 2 计划口径
- 更新本开发日志

### 验证结果

- 后端编译通过：`python -m compileall app`
- 前端构建通过：`npm run build`
- 后端接口验证通过：
  - `/api/health`
  - `/api/documents`
  - `/api/documents/{id}/preview`
  - `DELETE /api/documents/{id}`
- 文档格式补测通过：
  - Markdown 上传、解析、预览、删除通过
  - TXT 上传、解析、预览、删除通过
  - PDF 上传、解析、预览、删除通过
- 关键源码和 Markdown 文档完成乱码扫描，未发现残留坏编码。

### 遇到的问题

- `document_service.py` 和开发计划文档曾出现中文编码损坏，已整体重写修复。
- 阶段 2 原计划中的状态 `pending / processing / indexed / failed` 与当前同步实现不一致，已调整为更贴合基础 Demo 的 `processed / failed`。
- PowerShell 终端有时会把 UTF-8 中文响应显示成乱码，后续以源码文件内容和接口验证结果为准。

### 当前结论

- 阶段 1 已完成。
- 阶段 2 的基础版闭环已完成。
- 当前项目已经具备：
  - 基础聊天
  - 文档上传
  - 文档解析
  - 文档列表
  - 解析预览
  - 删除文档

### 下一步

- 进入阶段 3：RAG 核心闭环。
- 阶段 3 优先实现：
  - 文本切片
  - chunk metadata
  - Embedding
  - Chroma 入库
  - Top-K 检索
  - RAG Prompt
  - 基于文档回答
  - 引用来源
  - 无依据拒答

---

## 2026-07-08 阶段 2 最终复查与推送前整理

### 复查结论

- 阶段 2 已完成，可以进入阶段 3。
- 阶段 2 范围内没有未完成项。
- 文本切片、Embedding、Chroma 入库、Top-K 检索、RAG Prompt、引用来源和无依据拒答属于阶段 3，不计入阶段 2 未完成项。

### 复查内容

- 对照阶段 2 验收标准重新检查功能范围。
- 检查后端文档上传、列表、解析预览、删除接口。
- 检查前端知识库管理区的上传、刷新、查看解析、删除交互。
- 检查开发计划和阶段 2 状态口径是否一致。

### 修复内容

- 修复前端上传成功后文件选择框未完全清空的小问题。
- 上传成功后同时清空 React 状态和真实 file input 值，避免用户连续选择同一个文件时不触发变化。

### 验证结果

- 后端编译通过：`python -m compileall backend/app`
- 前端构建通过：`npm run build`
- Markdown 上传、解析、预览、删除通过。
- TXT 上传、解析、预览、删除通过。
- PDF 上传、解析、预览、删除通过。
- 非法格式上传返回 `400`，拦截正常。
- 删除测试文档后列表恢复为空。
- 关键文件按 UTF-8 检查正常。

### 下一步

- 提交并推送当前阶段成果。
- 下一阶段开始开发 RAG 核心闭环。

---

## 2026-07-08 阶段 3 基础 RAG 闭环开发

### 开发阶段

- 阶段 3：RAG 核心闭环

### 完成内容

- 新增基础 Embedding 服务。
  - 当前默认使用本地 deterministic embedding，保证无外部 API Key 时也能跑通 Demo。
  - 后续可以替换为真实 Embedding API。
- 新增 Chroma 向量库接入。
  - 本地持久化目录：`backend/data/chroma`
  - 上传文档解析成功后自动切片、向量化并写入 Chroma。
- 新增文本切片能力。
  - 支持固定长度切片。
  - 支持 chunk overlap。
  - 支持基础 metadata：`document_id`、`filename`、`chunk_index`、`page_number`、`created_at`。
- 新增 RAG 检索接口。
  - `POST /api/rag/search`
  - `POST /api/rag/reindex`
- 改造聊天接口。
  - 用户提问时先检索知识库。
  - 命中片段后将片段注入 Prompt。
  - 无命中片段时明确拒答，不编造。
  - 普通接口返回 `answer + sources`。
  - 流式接口先返回 `sources` 事件，再返回回答增量。
- 前端升级到 Phase 3。
  - 展示 RAG 引用来源。
  - 展示文档名、片段编号、页码和相似度。
  - 增加手动重建索引按钮。

### 关键变更

- 新增 `backend/app/services/embedding_service.py`
- 新增 `backend/app/services/rag_service.py`
- 新增 `backend/app/api/rag.py`
- 新增 `backend/app/schemas/rag.py`
- 更新 `backend/app/api/chat.py`
- 更新 `backend/app/services/llm_service.py`
- 更新 `backend/app/services/document_service.py`
- 更新 `backend/app/core/config.py`
- 更新 `backend/requirements.txt`，加入 `chromadb`
- 更新 `frontend/src/App.tsx`
- 更新 `frontend/src/styles.css`

### 验证结果

- 后端编译通过：`python -m compileall app`
- 前端构建通过：`npm run build`
- 空知识库提问时返回无依据拒答，`sources` 为空。
- 上传售后政策 TXT 后自动索引成功。
- RAG 检索能召回相关 chunk。
- 聊天接口能返回回答和引用来源。
- 删除文档后同步删除本地文档和向量索引。

### 遇到的问题

- 初次接入 Chroma 时，metadata 中的 `page_number=None` 导致 Chroma 写入失败；已改为存储 `0`，展示时再转换为空。
- Chroma 首次安装依赖较多，安装耗时较长；当前已安装成功。
- 为避免索引失败导致上传接口 500，已增加保护：解析成功但索引失败时，文档状态会变为 `failed` 并记录错误原因。

### 当前结论

- 阶段 3 的基础 RAG 闭环已经打通。
- 当前版本已经可以演示：上传文档 -> 自动切片入库 -> 用户提问 -> 检索相关片段 -> 基于片段回答 -> 展示引用来源 -> 无依据拒答。

### 下一步

- 继续完善阶段 3 的展示质量和 RAG 细节。
- 可选增强：
  - 更好的 chunk 展示。
  - 更精细的相似度阈值。
  - RAG 检索日志持久化。
  - 接入真实 Embedding API。
  - 准备一组标准测试文档和测试问题。

---

## 2026-07-08 阶段 3 Review 与补强

### Review 结论

- 阶段 3 基础 RAG 闭环已经完成。
- Review 中发现两个阶段 3 验收相关缺口，并已补齐：
  - 文档缺少索引状态和索引 chunk 数。
  - 缺少基础 RAG 检索日志。

### 修复内容

- 文档记录新增：
  - `index_status`
  - `indexed_chunks`
  - `index_error_message`
- 上传文档解析成功后，会记录索引结果。
- 前端文档列表展示“已索引 N 个片段”。
- 新增轻量 RAG 日志：
  - 查询问题
  - Top-K 参数
  - 命中来源数量
  - 命中的来源片段
  - 创建时间
- 新增接口：`GET /api/rag/logs`
- 将仓库根目录的 `key.txt` 加入 `.gitignore`，避免误提交本地密钥文件。

### 验证结果

- 后端编译通过：`python -m compileall backend/app`
- 前端构建通过：`npm run build`
- 上传售后政策 TXT 后：
  - `status=processed`
  - `index_status=indexed`
  - `indexed_chunks=1`
- 相关问题能召回知识片段。
- 聊天接口能返回回答和来源。
- 无关问题不召回来源，并触发无依据拒答。
- `/api/rag/logs` 可以返回检索日志。
- 删除测试文档后，文档列表为空。

### 当前结论

- 阶段 3 现在满足基础版验收要求。
- 后续仍可增强真实 Embedding、检索质量、RAG 日志后台页面和标准测试集，但这些不阻塞阶段 3 基础完成。

---

## 2026-07-10 阶段 3 RAG 正确性修复

### 完成内容

- 统一文档索引状态更新方法，上传自动索引和全量重建均通过同一入口持久化状态。
- 全量重建索引后同步更新 `index_status`、`indexed_chunks` 和 `index_error_message`。
- 单个文档重建失败时记录失败原因，并继续处理其余文档。
- 检索结果内部保留完整 chunk，真实模型 Prompt 改用完整内容。
- 完整 chunk 不返回浏览器、不写入轻量 RAG 日志；界面仍只展示截断预览。
- 前端文档列表明确区分“待索引”和“索引失败”。

### 验证结果

- 后端源码编译通过。
- 前端 TypeScript 检查和生产构建通过。
- 隔离测试确认：一个文档索引失败不会中断其他文档，成功与失败状态均正确落盘调用。
- 隔离测试确认：Prompt 包含完整 chunk，序列化响应不包含内部 `content` 字段。

### 下一步

- 增加可持续运行的后端自动化测试集，覆盖上传、检索、重建和删除闭环。
- 进入阶段 4：持久化会话、用户反馈、知识缺口和转人工请求。

---

## 2026-07-10 阶段 1-3 Review 问题修复

### 完成内容

- 移除低相似度结果通过两个重合中文字符绕过阈值的逻辑，所有召回结果现在必须满足 `RAG_MIN_SCORE`。
- 为文档元数据增加进程内可重入锁，上传、索引状态更新和删除的读改写过程不再互相覆盖。
- 文档元数据改为同目录临时文件写入、刷新落盘后原子替换，降低进程中断造成 JSON 文件损坏的风险。

### 验证结果

- 24 个文档并发更新测试通过，记录数量和索引状态均完整。
- 低相似度、仅有常见中文字符重合的无关文档不会被召回。
- 后端编译通过，前端生产构建通过。
- RAG 搜索参数增加边界校验：查询不能为空，`top_k` 限制为 1–20。
- RAG 日志增加进程内锁和原子写入，日志损坏时列表安全返回空结果。
- 日志读取、解析或写入失败不再中断知识库检索和聊天主流程。
- 非法参数、日志写入失败和 40 个并发日志写入的针对性测试通过。

---

## 2026-07-10 阶段 4 客服基础闭环开发

### 完成内容

- 新增会话和消息持久化，首次提问自动创建会话，后续提问可续接同一会话。
- 新增历史会话列表和会话详情接口，前端支持打开历史对话。
- 有帮助 / 没帮助按钮接入反馈接口，同一回复的反馈可以更新。
- 无知识库来源的回答完成后自动记录知识缺口。
- 无依据回答提供转人工入口并持久化待处理请求。
- 客服数据使用进程内锁、同目录临时文件和原子替换持久化。
- 前端升级为 Demo v0.4，并展示阶段 4 状态。

### 验证结果

- 后端编译通过。
- 前端 TypeScript 检查和生产构建通过。
- 隔离端到端测试覆盖：创建会话、续接会话、读取历史、提交反馈、记录知识缺口和申请转人工。
- 客服数据写入后无临时文件残留，差异格式检查通过。

### 下一步

- 为阶段 4 增加正式自动化测试文件，替代当前隔离验证脚本。
- 进入阶段 5，为反馈、知识缺口、转人工请求、会话和 RAG 日志提供管理后台。

---

## 2026-07-10 阶段 4 复查与阶段 5 管理后台开发

### 阶段 4 复查结论

- 前端实际使用的流式聊天链路验证通过。
- 流式回复完成后会话、AI 回复和知识缺口均正确持久化。
- 阶段 4 验收项完整，没有阻塞进入阶段 5 的遗漏。

### 阶段 5 完成内容

- 新增管理后台入口和 Dashboard。
- Dashboard 展示文档数量、今日会话、反馈、知识缺口和转人工请求五项指标。
- 新增反馈、知识缺口和转人工请求管理查询接口。
- 管理后台展示对话记录、反馈、知识缺口、转人工请求和 RAG 检索日志。
- 管理员可以打开对话详情，查看用户问题、AI 回复及引用文档片段。
- 知识库和文档管理继续复用现有工作台。

### 验证结果

- Dashboard 指标与隔离测试数据一致。
- 对话、反馈、知识缺口和转人工列表接口验证通过。
- 后端编译、前端 TypeScript 检查、生产构建和差异格式检查通过。

### 下一步

- 为知识缺口增加状态处理操作，为后台列表增加分页和筛选。
- 完成阶段 5 review 后进入阶段 6 Tool Calling / 企业系统模拟。

---

## 2026-07-10 阶段 5 Review、阶段 6 开发与整体回归

### 阶段 5 Review

- Dashboard、文档管理、对话记录与引用、反馈、知识缺口、转人工和 RAG 日志均已覆盖。
- 补齐知识缺口处理操作，支持“标记已解决”和“重新打开”。
- 阶段 5 最小管理闭环完成。

### 阶段 6 完成内容

- 新增 Mock 订单查询 API，内置订单 `10001`、`10002`、`10003`。
- 新增 Mock 工单创建 API，返回可追踪工单编号。
- 新增订单查询和工单创建意图识别。
- 工具结果转换为自然语言客服回复，并接入同步及流式聊天。
- 工具成功处理的问题不会误记为知识缺口。
- 新增使用锁和原子写入的工具调用日志，并在管理后台展示。
- 工具日志故障不会中断订单查询或工单创建主流程。

### 整体 Review 与验证

- 阶段 1-3：上传、解析、索引、检索、非法参数、重建和删除隔离回归通过。
- 阶段 4：流式会话持久化、反馈、知识缺口、转人工和历史会话通过。
- 阶段 5：Dashboard 指标、管理列表、对话引用详情和知识缺口处理通过。
- 阶段 6：订单 API、工单 API、同步工具聊天、流式工具聊天和日志通过。
- 后端编译、前端 TypeScript 检查、生产构建和差异格式检查通过。
- Windows 隔离测试清理 Chroma 临时目录时可能短暂出现文件句柄占用提示，不影响业务断言和正常持久化目录。

### 后续增强

- 增加正式的 pytest 自动化测试套件和前端组件测试。
- 为后台列表增加分页、搜索和筛选。
- 增加真实身份认证和管理员接口授权；当前 Demo 仅提供用户/管理员视图区分。
- 增加 Docker Compose、演示数据和标准问题测试集。

---

## 2026-07-10 扫描 PDF 上传解析修复

### 问题原因

- 原解析器仅使用 `pypdf` 提取 PDF 文本层。
- 用户上传的 PDF 为扫描件，没有可提取文本层，因此上传成功但文档状态为解析失败。

### 修复内容

- 增加 PyMuPDF 页面渲染和 RapidOCR OCR 回退。
- 文本 PDF 继续使用原快速路径，仅对无文本页面执行 OCR。
- 混合型 PDF 支持逐页选择文本提取或 OCR。
- OCR 最多处理前 50 页，避免超大扫描文档长期占用服务。
- 新增失败文档“原文件 OCR 重试”接口和前端按钮，无需重新上传。
- OCR 解析成功后自动写入文本、切片、建立 Chroma 索引并更新文档状态。

### 验证结果

- 原失败扫描 PDF OCR 成功，解析得到 2391 字并索引 5 个片段。
- 第二份扫描 PDF 通过页面按钮重试成功，解析得到 3697 字并索引 8 个片段。
- 第三份护照扫描 PDF 重试成功，解析得到 893 字并索引 2 个片段。
- 解析预览正确显示中英文内容。
- 查询“无犯罪记录证明”成功召回 OCR 文档片段。
- 后端编译、前端生产构建和差异格式检查通过。

---

## 2026-07-10 多格式文档解析、清洗与向量入库增强

### 支持格式

- PDF 文本层提取与扫描页 OCR
- PNG / JPG / JPEG / WebP / BMP / TIFF 文字图片 OCR
- UTF-8 及自动编码识别 TXT、Markdown、CSV
- DOCX 段落、标题和表格顺序提取
- XLSX、XLS 工作表及行列结构提取

### 数据清洗

- Unicode NFKC 规范化
- 清理 NUL、零宽字符和不可见控制字符
- 保留表格制表符和段落结构
- PDF 重复页眉、页脚和变化页码清理
- Word 页眉页脚不进入正文，标题转换为 Markdown 层级
- Excel 保留工作表标记、公式文本、日期和值的行列结构
- Office/PDF 文件签名校验，对 Git LFS 指针给出明确错误

### RAG 增强

- 扩大向量候选池并增加严格词项覆盖混合召回。
- 解决大 TXT 文档淹没 Excel 精确表头的问题。
- 无 50% 词项覆盖时，本地哈希向量至少达到 0.30 才允许召回。
- 保留“退款政策”不会误召回“办公时间”的负例保护。

### 公开样本验证

- W3C PDF：23 字、1 个 chunk、检索通过。
- RapidOCR 官方 JPG：106 字、1 个 chunk、OCR 与检索通过。
- Project Gutenberg TXT：144039 字、294 个 chunks、检索通过。
- python-docx 官方 DOCX：20 字、段落/表格顺序正确、检索通过。
- Microsoft XLSX：82913 字、167 个 chunks、工作表/表格与检索通过。
- FileSamples XLS：1282 字、3 个 chunks、旧版工作簿与检索通过。

### 迭代中修复的问题

- 替换实际为 Git LFS 指针的错误 Excel 测试样本。
- 修复 Excel 在大 TXT 同库时无法进入向量 Top-K。
- 修复短 PDF 页面正文被页眉页脚算法重复计数并删除。
- OCR、Office 解析和索引移入工作线程，OCR 上传期间健康接口持续可用。
- 固定与 Chroma 0.5.23 兼容的 PostHog 版本，清除遥测 API 警告。

### 最终验证

- 六格式 API 上传、预览、清洗、索引、检索和删除全量重复测试通过。
- 7 项自动化回归测试通过。
- 用户原有 3 份文档共 15 个 chunks，Chroma 实际 15 个，无测试残留或孤儿向量。
- 后端编译、前端 TypeScript/生产构建和差异格式检查通过。
